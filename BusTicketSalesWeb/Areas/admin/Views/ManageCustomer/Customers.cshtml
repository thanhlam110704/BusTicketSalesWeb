@model IEnumerable<BusTicketSalesWeb.Areas.ViewModel.KhachHangListViewModel>

@{
    ViewBag.Title = "Customers";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}

@section CSS {
    <!-- DataTables -->
    <link rel="stylesheet" href="~/Areas/admin/wwwroot/plugins/datatables-bs4/css/dataTables.bootstrap4.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        table th,
        table td {
            text-align: center;
        }
    </style>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0 text-dark">Khách hàng</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/">Trang Chủ</a></li>
                    <li class="breadcrumb-item active">Khách hàng</li>
                </ol>
            </div>
        </div>
    </div>
    <!-- /.container-fluid -->
</section>
<!-- /.content-header -->
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Thông tin khách hàng</h3>
                </div>
                <!-- Card Body -->
                <div class="card-body">
                    <div class="input-group input-group-sm ml-auto" style="width: 185px;">
                        <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default"><i class="fas fa-search"></i></button>
                        </div>
                    </div>
                    <table id="example2" class="table table-bordered table-hover" style="background-color: #f2edf3">
                        <thead>
                            <tr>
                                <th>Mã khách hàng</th>
                                <th>Họ tên</th>
                               
                                <th>Số điện thoại </th>
                                <th>Giới tính</th>
                                <th>Ngày sinh</th>
                                <th>Số lượng vé đã mua</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@{ var STT = 1; }*@
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td style="width:150px">
                                        @Html.DisplayFor(modelItem => item.MaKH)
                                    </td>
                                    <td style="text-align: start;width:250px">
                                        @Html.DisplayFor(modelItem => item.TenKH)
                                    </td>                                 
                                    <td style="text-align: start">
                                        @Html.DisplayFor(modelItem => item.Sdt)
                                    </td>
                                    <td>
                                        @if (item.Gioi_Tinh.HasValue)
                                        {
                                            <span>@(item.Gioi_Tinh.Value == 1 ? "Nam" : "Nữ")</span>
                                        }
                                    </td>
                                    <td>
                                        @String.Format("{0:dd/MM/yyyy}", item.Ngay_Sinh)
                                    </td>
                                    <td style="text-align: start">
                                        @*@Html.DisplayFor(modelItem => item.Sdt)*@
                                    </td>

                                    <td style="width:120px;height:50px">
                                        @Html.ActionLink("Chỉnh sửa", "EditCustomer", new { id = item.MaKH }, new { @class = "btn btn-warning btn-sm", @style = "color:white;font-weight:500" })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- /.container-fluid -->
<!-- /.content -->

@section Scripts {
    <!-- DataTables -->
    <script src="~/Areas/admin/wwwroot/plugins/datatables/jquery.dataTables.js"></script>
    <script src="~/Areas/admin/wwwroot/plugins/datatables-bs4/js/dataTables.bootstrap4.js"></script>

    <!-- page script -->

}